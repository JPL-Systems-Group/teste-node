{
  "name": "order-processor",
  "version": "2.0.0",
  "description": "Production-ready order processor microservice",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --experimental-test-coverage",
    "test:ci": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "amqplib": "^0.10.5",
    "pg": "^8.13.1"
  }
}
